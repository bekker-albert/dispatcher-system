(function(){
  const TODAY_KEY=dayKey(today);
  const style=document.createElement('style');style.id='v27-styles';style.textContent=`.assistant-top-btn{font-size:19px;color:var(--primary);background:rgba(125,101,255,.13)!important}.today-notes{grid-template-columns:1fr!important}.today-note{min-height:0!important;width:100%}.finance-actions-v27{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin:10px 0 14px}.finance-actions-v27 button{border:1px solid var(--border);border-radius:13px;background:var(--surface-2);color:var(--text);padding:11px;font-weight:800}.finance-actions-v27 button.primary{color:var(--primary);background:rgba(125,101,255,.12)}.budget-detail-v27{margin:12px 0}.analytics-tabs-v27{display:grid;grid-template-columns:repeat(3,1fr);gap:7px;margin:8px 0 14px}.analytics-tabs-v27 button{border:1px solid var(--border);background:var(--surface-2);color:var(--muted);padding:9px 7px;border-radius:12px;font-weight:800;font-size:12px}.analytics-tabs-v27 button.active{color:var(--primary);background:rgba(125,101,255,.13)}.income-source-note{padding:9px 10px;border-radius:11px;background:rgba(55,207,123,.09);font-size:11px;color:var(--muted);margin-bottom:10px}.plan-day-title{margin:11px 0 4px;color:var(--muted);font-size:12px}`;document.head.appendChild(style);
  state.analyticsSection=state.analyticsSection||'tasks';

  const oldGetEntity=getEntity;getEntity=function(type,id){if(type==='budgetAmount'||type==='budgetAllocation')return id?state.budgets.find(x=>x.id===id)||null:null;return oldGetEntity(type,id)};window.getEntity=getEntity;
  const oldEditorTitle=editorTitle;editorTitle=function(type,id){if(type==='budgetAmount')return 'Бюджет месяца';if(type==='budgetAllocation')return 'Распределение бюджета';return oldEditorTitle(type,id)};window.editorTitle=editorTitle;
  function allocationRow(row={}){return `<div class="budget-allocation-row">${sel('allocationCategory','Статья',categoryOptions('expense',row.category||''))}${f('allocationAmount','Сумма, ₸',row.amount||'','number','min="0" step="0.01"')}<button type="button" aria-label="Удалить строку" onclick="this.closest('.budget-allocation-row').remove()">×</button></div>`}
  function addAllocation(row={}){document.querySelector('#budget-allocation-list')?.insertAdjacentHTML('beforeend',allocationRow(row))}window.addBudgetAllocationRowV27=addAllocation;
  const oldFields=editorFields;editorFields=function(type,item={},extra={}){
    if(type==='income'){const date=item.date||extra.date||TODAY_KEY,isFuture=date>TODAY_KEY,status=item.status||(isFuture?'planned':'actual'),notify=item.notifyEnabled!==undefined?Boolean(item.notifyEnabled):Boolean(item.reminders?.length);return `<input type="hidden" name="status" value="${esc(status)}"><input type="hidden" name="category" value="${esc(item.category||state.categories.find(x=>x.type==='income')?.id||'other-income')}">${f('sourceName','Источник дохода',item.sourceName||item.note||'','text','required placeholder="Например: заработная плата, возврат, аренда"')}<div class="income-source-note">Источник указывается вручную и отображается как название операции.</div>${f('amount','Сумма, ₸',item.amount||'','number','required min="0.01" step="0.01"')}<div class="field-row">${f('date','Дата',date,'date','required onchange="refreshTransactionFormV26()"')}${sel('account','Счёт',accountOptions(item.account||state.accounts[0]?.id))}</div>${ta('note','Комментарий',item.note&&item.note!==item.sourceName?item.note:'',3)}<div class="tx-state-note" id="tx-state-note"><span class="tx-state-icon">${status==='actual'?'✓':'◷'}</span><div><strong id="tx-state-title">${status==='actual'?'Будет учтено как полученный доход':'Будет сохранено как ожидаемый доход'}</strong><small id="tx-state-help">${status==='actual'?'Для полученного дохода время не требуется.':'Напоминание можно включить отдельно.'}</small></div></div><label class="check-option tx-reminder-toggle" id="tx-reminder-toggle"><input type="checkbox" name="notifyEnabled" ${notify?'checked':''} onchange="refreshTransactionFormV26()"><span>Напомнить о поступлении</span></label><div class="tx-reminder-panel" id="tx-reminder-panel" ${notify?'':'hidden'}>${f('time','Время напоминания',item.time||'09:00','time')}<label class="field"><span>Когда напомнить</span>${reminderGrid(item.reminders?.length?item.reminders:[1440,60])}</label></div>`}
    if(type==='budgetAmount')return `${f('name','Название бюджета',item.name||'Основной бюджет','text','required')}${f('month','Месяц',item.month||state.financeMonth,'month','required')}${f('amount','Сумма бюджета, ₸',item.amount||'','number','required min="0" step="0.01"')}<p class="muted">Здесь указывается только общая сумма. Распределение выполняется отдельно.</p>`;
    if(type==='budgetAllocation'){const rows=Array.isArray(item.allocations)?item.allocations:[];return `<input type="hidden" name="month" value="${esc(item.month||state.financeMonth)}"><div class="budget-editor-note">Распределите уже внесённый бюджет по существующим статьям расходов.</div><div id="budget-allocation-list" class="budget-allocation-list">${rows.map(allocationRow).join('')}</div><button type="button" class="secondary budget-add-row" onclick="addBudgetAllocationRowV27()">＋ Добавить строку</button>`}
    return oldFields(type,item||{},extra||{})};window.editorFields=editorFields;
  function readAllocations(form){const map=new Map();form.querySelectorAll('.budget-allocation-row').forEach(row=>{const c=row.querySelector('[name=allocationCategory]')?.value||'',a=Math.max(0,Number(row.querySelector('[name=allocationAmount]')?.value||0));if(c)map.set(c,(map.get(c)||0)+a)});return [...map.entries()].map(([category,amount])=>({category,amount}))}
  const oldSubmit=submitEditor;submitEditor=function(form){
    if(editor.type==='income'){const fd=new FormData(form),get=k=>String(fd.get(k)||'').trim(),amount=Number(fd.get('amount')||0),date=get('date'),sourceName=get('sourceName');if(!sourceName){showEditorError('Укажите название источника дохода.');return}if(!(amount>0)){showEditorError('Укажите сумму больше нуля.');return}if(!date){showEditorError('Укажите дату.');return}const existing=editor.id?state.transactions.find(x=>x.id===editor.id):null,notify=Boolean(form.elements.notifyEnabled?.checked&&!document.querySelector('#tx-reminder-toggle')?.hidden),data={type:'income',sourceName,amount,date,account:get('account'),category:get('category')||state.categories.find(x=>x.type==='income')?.id||'other-income',status:get('status')||(date>TODAY_KEY?'planned':'actual'),time:notify?(get('time')||'09:00'):'09:00',recurrence:{type:'none',interval:1},note:get('note'),notifyEnabled:notify,reminders:notify?checkedReminders(form):[],settledDates:existing?.settledDates||[],createdAt:existing?.createdAt||Date.now()};if(existing)Object.assign(existing,data);else state.transactions.push({...data,id:uid('tx')});save();closeDialog('editor-dialog');toast('Доход сохранён');render();return}
    if(editor.type==='budgetAmount'){const fd=new FormData(form),name=String(fd.get('name')||'').trim(),month=String(fd.get('month')||''),amount=Number(fd.get('amount')||0);if(!name||!month||amount<0){showEditorError('Проверьте название, месяц и сумму бюджета.');return}let budget=editor.id?state.budgets.find(x=>x.id===editor.id):state.budgets.find(x=>x.month===month);if(budget)Object.assign(budget,{name,month,amount,updatedAt:Date.now()});else state.budgets.push({id:uid('budget'),name,month,amount,allocations:[],createdAt:Date.now()});state.financeMonth=month;save();closeDialog('editor-dialog');toast('Бюджет внесён');render();return}
    if(editor.type==='budgetAllocation'){const budget=editor.id?state.budgets.find(x=>x.id===editor.id):state.budgets.find(x=>x.month===state.financeMonth);if(!budget){showEditorError('Сначала внесите бюджет месяца.');return}const allocations=readAllocations(form),allocated=allocations.reduce((s,x)=>s+x.amount,0);if(allocated>Number(budget.amount||0)){showEditorError(`Распределено больше бюджета на ${money(allocated-Number(budget.amount||0))}.`);return}budget.allocations=allocations;budget.updatedAt=Date.now();save();closeDialog('editor-dialog');toast('Распределение сохранено');render();return}
    oldSubmit(form)};window.submitEditor=submitEditor;
  function txTitle(tx){return tx.type==='income'?(tx.sourceName||tx.note||category(tx.category)?.name||'Доход'):(category(tx.category)?.name||tx.note||'Расход')}
  transactionCard=function(tx,key){const cat=category(tx.category),settled=txSettled(tx,key),planned=!settled,title=txTitle(tx),meta=[dateFmt(key,{day:'2-digit',month:'2-digit'}),account(tx.account)?.name||'',planned&&tx.notifyEnabled?`🔔 ${tx.time||'09:00'}`:''].filter(Boolean).join(' · ');return `<article class="card transaction v26"><div class="tx-icon" style="color:${cat?.color||'#aaa'}">${tx.type==='income'?'+':'−'}</div><div class="tx-main" onclick="openEditor('${tx.type}','${tx.id}')"><h4>${esc(title)}</h4><p>${esc(meta)}</p><span class="tx-status ${planned?'planned':'actual'}">${planned?'Запланировано':tx.type==='expense'?'Оплачено':'Получено'}</span></div><strong class="${tx.type==='income'?'positive':'negative'}">${tx.type==='income'?'+':'−'}${money(tx.amount)}</strong><button type="button" class="menu-btn" onclick="event.stopPropagation();transactionMenuV27('${tx.id}','${key}')">⋮</button></article>`};window.transactionCard=transactionCard;
  function transactionMenuV27(id,key){const tx=state.transactions.find(x=>x.id===id);if(!tx)return;window.__txActionContextV26={id,key};const dialog=ensureTransactionActionsDialogV26(),settled=txSettled(tx,key);dialog.querySelector('#tx-sheet-title').textContent=txTitle(tx);dialog.querySelector('#tx-sheet-meta').textContent=`${money(tx.amount)} · ${dateFmt(key,{day:'numeric',month:'long'})}`;dialog.querySelector('#tx-actions-grid').innerHTML=`<button type="button" class="task-action" data-tx-action="edit"><span>✎</span><b>Изменить</b></button>${!settled?`<button type="button" class="task-action" data-tx-action="settle"><span>✓</span><b>${tx.type==='expense'?'Оплачено':'Получено'}</b></button>`:''}<button type="button" class="task-action" data-tx-action="duplicate"><span>⧉</span><b>Дублировать</b></button><button type="button" class="task-action danger" data-tx-action="delete"><span>×</span><b>Удалить</b></button>`;dialog.querySelectorAll('[data-tx-action]').forEach(button=>button.onclick=()=>runTransactionActionV26(button.dataset.txAction));try{if(!dialog.open)dialog.showModal()}catch{dialog.setAttribute('open','')}}window.transactionMenuV27=transactionMenuV27;
  function renderPlanV27(){const key=state.selectedDate,d=new Date(key+'T12:00:00'),days=Array.from({length:21},(_,i)=>addDays(d,i-10)),tasks=tasksForDate(key),transactions=transactionsForDate(key),workouts=workoutsForDate(key);document.querySelector('#plan').innerHTML=`<div class="plan-toolbar"><button type="button" onclick="moveSelectedDate(-1)">‹</button><button type="button" class="today-btn" onclick="selectDate('${TODAY_KEY}')">${dateFmt(d,{day:'numeric',month:'long',year:'numeric'})}</button><button type="button" onclick="moveSelectedDate(1)">›</button></div><div class="date-strip" id="date-strip">${days.map(date=>`<button type="button" data-date="${dayKey(date)}" class="date-chip ${dayKey(date)===key?'selected':''}" onclick="selectDate('${dayKey(date)}')"><span>${dateFmt(date,{weekday:'short'})}</span><strong>${date.getDate()}</strong></button>`).join('')}</div><p class="plan-day-title">Повестка выбранного дня</p>${agendaView(key,tasks,transactions,workouts)}`;requestAnimationFrame(()=>document.querySelector('#date-strip .selected')?.scrollIntoView({behavior:'auto',inline:'center',block:'nearest'}))}
  function setAnalyticsSectionV27(section){state.analyticsSection=section;save();render()}window.setAnalyticsSectionV27=setAnalyticsSectionV27;
  function renderAnalyticsV27(){const days=state.analyticsRange,taskData=taskAnalyticsV26(days),tripData=tripAnalyticsV26(days),section=state.analyticsSection,tabs=`<div class="analytics-tabs-v27"><button type="button" class="${section==='tasks'?'active':''}" onclick="setAnalyticsSectionV27('tasks')">Задачи</button><button type="button" class="${section==='trips'?'active':''}" onclick="setAnalyticsSectionV27('trips')">Командировки</button><button type="button" class="${section==='projects'?'active':''}" onclick="setAnalyticsSectionV27('projects')">Проекты</button></div><div class="analytics-range">${[7,30,90].map(n=>`<button type="button" class="${days===n?'active':''}" onclick="setAnalyticsRange(${n})">${n} дней</button>`).join('')}</div>`;let body='';if(section==='trips')body=`<div class="analytics-v26-grid"><article class="card analytics-v26-card"><small>Командировок</small><strong>${tripData.count}</strong><small>в выбранном окне</small></article><article class="card analytics-v26-card"><small>Дней</small><strong>${tripData.tripDays}</strong><small>в командировках</small></article><article class="card analytics-v26-card"><small>Активно</small><strong>${tripData.active}</strong><small>сейчас</small></article><article class="card analytics-v26-card"><small>Ближайшие</small><strong>${tripData.upcoming}</strong><small>запланировано</small></article><article class="card analytics-v26-card wide"><small>Текущие и ближайшие</small><div class="analytics-trip-list">${tripData.nextTrips.length?tripData.nextTrips.map(task=>`<div class="analytics-trip-row" onclick="openEditor('task','${task.id}')"><div><strong>${esc(task.title)}</strong><small>${dateFmt(task.date,{day:'2-digit',month:'2-digit'})}–${dateFmt(task.endDate||task.date,{day:'2-digit',month:'2-digit',year:'numeric'})}</small></div><b>›</b></div>`).join(''):'<div class="empty">Командировок нет.</div>'}</div></article></div>`;else if(section==='projects'){const rows=[...state.projects].map(p=>{let planned=0,done=0;for(let i=0;i<days;i++){const key=dayKey(addDays(today,-i));tasksForDate(key).filter(t=>t.project===p.id).forEach(t=>{planned++;if(taskDone(t,key))done++})}return {p,planned,done,pct:planned?Math.round(done/planned*100):0}}).sort((a,b)=>b.planned-a.planned);body=`<div class="analytics-list">${rows.length?rows.map(x=>`<div class="analytics-row" onclick="openProject('${x.p.id}')"><span class="label">${esc(x.p.name)}</span><span class="track"><i style="width:${x.pct}%"></i></span><small>${x.done}/${x.planned}</small></div>`).join(''):'<div class="card empty">Проектов нет.</div>'}</div>`}else body=`<div class="analytics-v26-grid"><article class="card analytics-v26-card"><small>Выполнение</small><strong>${taskData.percent}%</strong><small>${taskData.done} из ${taskData.planned}</small></article><article class="card analytics-v26-card"><small>Просрочено</small><strong>${taskData.overdue}</strong><small>уникальных записей</small></article><article class="card analytics-v26-card"><small>Закрытая нагрузка</small><strong>${Math.round(taskData.doneMinutes/60)} ч</strong><small>из ${Math.round(taskData.minutes/60)} ч</small></article><article class="card analytics-v26-card"><small>Запланировано</small><strong>${taskData.planned}</strong><small>выполнений</small></article></div>`;document.querySelector('#analytics').innerHTML=tabs+body}
  function renderFinanceV27(){const list=monthTransactions(state.financeMonth),budget=monthBudgetV26(state.financeMonth),stats=budgetStatsV26(budget,state.financeMonth),income=list.filter(x=>x.type==='income'&&x.settled).reduce((s,x)=>s+Number(x.amount||0),0),totalBalance=state.accounts.filter(x=>x.active!==false).reduce((s,x)=>s+accountBalance(x.id),0);document.querySelector('#finance').innerHTML=`<div class="plan-toolbar"><button type="button" onclick="moveFinanceMonth(-1)">‹</button><button type="button" class="today-btn">${dateFmt(state.financeMonth+'-01',{month:'long',year:'numeric'})}</button><button type="button" onclick="moveFinanceMonth(1)">›</button></div><article class="card budget-center-summary"><small>${budget?esc(budget.name):'Бюджет месяца не внесён'}</small><div class="budget-main">${money(stats.amount)}</div><div class="budget-kpis"><div class="budget-kpi"><small>Распределено</small><strong>${money(stats.allocated)}</strong></div><div class="budget-kpi"><small>Свободно</small><strong>${money(stats.free)}</strong></div><div class="budget-kpi"><small>Расход факт</small><strong>${money(stats.actual)}</strong></div><div class="budget-kpi"><small>Остаток</small><strong>${money(stats.remaining)}</strong></div></div></article><div class="finance-actions-v27"><button type="button" class="primary" onclick="openEditor('budgetAmount','${budget?.id||''}')">${budget?'Изменить бюджет':'Внести бюджет'}</button><button type="button" onclick="${budget?`openEditor('budgetAllocation','${budget.id}')`:`toast('Сначала внесите бюджет')`}">Распределить</button></div><div class="budget-detail-v27">${sectionHeadV26('Исполнение по статьям',budget?.allocations?.length||0,[])}${allocationCardsV26(budget,stats)}</div><div class="finance-hero"><small>Общий остаток по счетам</small><div class="money-main">${money(totalBalance)}</div><div class="money-row"><div class="money-mini"><span>Доход факт</span><strong class="positive">${money(income)}</strong></div><div class="money-mini"><span>Расход факт</span><strong class="negative">${money(stats.actual)}</strong></div></div></div>${sectionHeadV26('Операции',list.length,[{label:'＋ расход',action:"openEditor('expense')",type:'expense',primary:true},{label:'＋ доход',action:"openEditor('income')",type:'income'}])}${list.length?list.sort((a,b)=>b.occurrenceDate.localeCompare(a.occurrenceDate)).map(x=>transactionCard(x,x.occurrenceDate)).join(''):'<div class="card empty">Операций пока нет.</div>'}`}
  const oldOpenMore=openMore;openMore=function(view){if(view==='budget'){setPage('finance');return}oldOpenMore(view)};window.openMore=openMore;
  headings.analytics=['Аналитика','Задачи, командировки и проекты'];headings.plan=['План','Календарь и повестка'];headings.finance=['Финансы','Бюджет, распределение и операции'];
  setPage=function(next){page=next;document.querySelectorAll('.page').forEach(node=>node.classList.toggle('active',node.id===next));document.querySelectorAll('.nav-item').forEach(node=>node.classList.toggle('active',node.dataset.page===next));const h=headings[next]||[next,''];document.querySelector('#page-title').textContent=h[0];document.querySelector('#page-subtitle').textContent=h[1];render()};window.setPage=setPage;
  const oldRender=render;render=function(){oldRender();if(page==='plan')renderPlanV27();if(page==='analytics')renderAnalyticsV27();if(page==='finance')renderFinanceV27();document.querySelectorAll('.nav-item').forEach(button=>button.onclick=()=>setPage(button.dataset.page));const assistantButton=document.querySelector('#assistant-btn');if(assistantButton)assistantButton.onclick=()=>setPage('assistant');document.querySelector('#more [data-module="budget"]')?.remove();const footer=[...document.querySelectorAll('#more p')].find(node=>node.textContent.includes('Штаб AI'));if(footer)footer.textContent='Штаб AI · 2.7.0'};window.render=render;
  document.querySelector('#assistant-btn').onclick=()=>setPage('assistant');if(native()&&!localStorage.getItem('shtab-ai-permissions-requested')){localStorage.setItem('shtab-ai-permissions-requested','1');setTimeout(()=>{try{Android.requestInitialPermissions()}catch{}},500)}
  try{Object.defineProperty(window,'state',{configurable:true,get:()=>state})}catch{}
  window.__shtabDiagnostics={...(window.__shtabDiagnostics||{}),financeMonth:()=>state.financeMonth,todayKey:()=>dayKey(today),transactionTitle:txTitle,analyticsSection:()=>state.analyticsSection};render();
})();
//# sourceURL=chunk20.js