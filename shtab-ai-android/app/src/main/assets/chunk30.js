(function(){
  const TODAY=dayKey(today);
  const KIND={task:{icon:'✓',name:'Задача',group:'tasks'},assignment:{icon:'↗',name:'Поручение',group:'tasks'},trip:{icon:'✈',name:'Командировка',group:'trips'},event:{icon:'◉',name:'Событие',group:'events'}};
  const GROUP_NAME={tasks:'Задачи',trips:'Командировки',events:'События'};
  const GROUP_DONE={tasks:'выполнено',trips:'завершено',events:'проведено'};
  const style=document.createElement('style');style.id='v40-styles';style.textContent=`
    #finance,[data-page="finance"]{display:none!important}.bottom-nav{grid-template-columns:repeat(4,minmax(0,1fr))!important}
    .today-icon-grid.v40{grid-template-columns:repeat(5,minmax(0,1fr));gap:4px}.today-icon-grid.v40 .today-icon-stat{min-width:0;padding:5px 0}.today-icon-grid.v40 .today-icon-stat i{width:30px;height:30px;margin-bottom:4px}.today-icon-grid.v40 .today-icon-stat b{font-size:12px}.today-icon-grid.v40 .today-icon-stat small{font-size:8px;overflow:hidden;text-overflow:ellipsis}
    .check-v40{width:24px!important;height:24px!important;border-radius:50%!important;border:1.5px solid #697286!important;background:rgba(255,255,255,.025)!important;color:#aeb5c5!important;display:grid!important;place-items:center!important;padding:0!important;font-size:13px!important;font-weight:900!important;line-height:1!important;flex:0 0 auto}.check-v40.done{background:var(--primary)!important;border-color:var(--primary)!important;color:#fff!important;box-shadow:0 0 0 3px rgba(125,101,255,.12)}
    .important-v40{grid-template-columns:24px minmax(0,1fr) 24px!important;gap:9px!important;align-items:center!important}.important-mark-v40{width:22px;height:22px;border-radius:8px;display:grid;place-items:center;font-size:15px;font-weight:950;color:#ff8790;background:rgba(255,109,119,.13);border:1px solid rgba(255,109,119,.34)}
    .priority-v40{width:20px;height:20px;border-radius:7px;display:grid;place-items:center;flex:0 0 auto;font-size:11px;font-weight:950;border:1px solid var(--border);background:var(--surface-2);color:var(--muted)}.priority-v40.high{font-size:15px;color:#ff8790;border-color:rgba(255,109,119,.34);background:rgba(255,109,119,.13)}.priority-v40.normal{color:var(--orange)}
    .analytics-tools-v40{display:flex;gap:6px;overflow-x:auto;padding:2px 0 8px;scrollbar-width:none}.analytics-tools-v40::-webkit-scrollbar{display:none}.analytics-tools-v40 button{border:1px solid var(--border);background:var(--surface);color:var(--muted);border-radius:11px;padding:8px 11px;white-space:nowrap;font-size:10px;font-weight:850}.analytics-tools-v40 button.active{background:rgba(125,101,255,.17);border-color:rgba(125,101,255,.5);color:#ddd7ff}
    .analytics-summary-v40{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:7px;margin:4px 0 10px}.summary-v40{border:1px solid var(--border);background:linear-gradient(145deg,rgba(39,43,58,.96),rgba(20,23,33,.96));border-radius:16px;padding:11px 8px;min-width:0}.summary-v40 small{display:block;color:var(--muted);font-size:9px;line-height:1.25;min-height:23px}.summary-v40 strong{display:block;font-size:21px;margin-top:4px;line-height:1}.summary-v40 .good{color:var(--green)}.summary-v40 .bad{color:var(--red)}
    .category-grid-v40{display:grid;gap:8px;margin-bottom:11px}.category-v40{border:1px solid var(--border);background:var(--surface);border-radius:17px;padding:12px}.category-v40.active{border-color:rgba(125,101,255,.55);box-shadow:0 0 0 2px rgba(125,101,255,.08)}.category-head-v40{display:grid;grid-template-columns:32px minmax(0,1fr) auto;gap:9px;align-items:center}.category-icon-v40{width:32px;height:32px;border-radius:11px;display:grid;place-items:center;background:rgba(125,101,255,.13);color:var(--primary);font-size:14px;font-weight:900}.category-head-v40 strong{display:block;font-size:13px}.category-head-v40 small{display:block;color:var(--muted);font-size:9px;margin-top:3px}.category-pct-v40{font-size:19px;font-weight:900}.progress-v40{height:6px;border-radius:10px;background:var(--surface-2);overflow:hidden;margin-top:10px}.progress-v40 span{display:block;height:100%;border-radius:10px;background:linear-gradient(90deg,var(--primary),#55d6ff)}.category-foot-v40{display:flex;justify-content:space-between;gap:8px;color:var(--muted);font-size:9px;margin-top:7px}.category-foot-v40 .late{color:var(--red)}
    .trend-v40{border:1px solid var(--border);border-radius:17px;background:var(--surface);padding:12px;margin-bottom:11px}.trend-v40 h3{font-size:13px;margin:0 0 10px}.bars-v40{display:grid;grid-template-columns:repeat(7,minmax(0,1fr));gap:6px;height:90px;align-items:end}.bar-wrap-v40{height:100%;display:flex;flex-direction:column;justify-content:flex-end;align-items:center;gap:5px}.bar-v40{width:100%;max-width:25px;min-height:4px;border-radius:7px 7px 3px 3px;background:linear-gradient(180deg,#8c72ff,#4a37be)}.bar-wrap-v40 b,.bar-wrap-v40 small{font-size:8px}.bar-wrap-v40 small{color:var(--muted)}
    .completed-v40{border:1px solid var(--border);border-radius:17px;background:var(--surface);overflow:hidden;padding:0 10px}.completed-row-v40{display:grid;grid-template-columns:24px 28px minmax(0,1fr) auto;gap:8px;align-items:center;padding:11px 0;border-bottom:1px solid var(--border)}.completed-row-v40:last-child{border-bottom:0}.completed-row-v40>i{width:27px;height:27px;border-radius:9px;display:grid;place-items:center;background:var(--surface-2);color:var(--primary);font-style:normal;font-size:11px}.completed-row-v40>div{min-width:0}.completed-row-v40 strong{display:block;font-size:12px;line-height:1.3;overflow-wrap:anywhere}.completed-row-v40 small{display:block;color:var(--muted);font-size:9px;margin-top:4px}.completed-row-v40>b{font-size:9px;color:var(--green);white-space:nowrap}
  `;document.head.appendChild(style);

  const kind=item=>KIND[item?.kind]||KIND.task;
  const dateLabel=value=>dateFmt(value,{day:'2-digit',month:'short',year:'numeric'});
  const period=item=>{const start=item.date||TODAY,end=item.endDate||start;return start===end?dateLabel(start):`${dateLabel(start)} — ${dateLabel(end)}`};
  const timeLabel=item=>{const start=item.startTime||item.time||'',end=item.endTime||'';return start?`${start}${end?`–${end}`:''}`:'Без времени'};
  function check(item,key,extra=''){const done=taskDone(item,key);return `<button type="button" class="task-check task-check-v31 check-v40 ${done?'done':''} ${extra}" data-complete-task-id="${item.id}" data-complete-key="${key}" aria-label="${done?'Вернуть в работу':'Отметить выполненным'}" onclick="event.stopPropagation();toggleTask('${item.id}','${key}')">✓</button>`}
  function section(title,count,body,action=''){return `<section class="simple-window-v30"><div class="simple-window-head-v30"><h3>${esc(title)}</h3><div><small>${count}</small>${action}</div></div><div class="simple-window-list-v30">${body||'<div class="simple-empty-v30">Записей нет.</div>'}</div></section>`}
  const noteRow=item=>`<div class="plan-entry-v30" onclick="openEditor('note','${item.id}')"><span class="plan-entry-icon-v30">✎</span><div class="plan-entry-main-v30"><strong>${esc(item.title)}</strong><small>${esc((item.body||'Без текста').replace(/\s+/g,' ').slice(0,80))}</small></div><span>›</span></div>`;
  const workoutRow=item=>`<div class="plan-entry-v30" onclick="openEditor('workout','${item.id}')"><span class="plan-entry-icon-v30">⚡</span><div class="plan-entry-main-v30"><strong>${esc(item.title)}</strong><small>${dateLabel(item.date)}${item.time?` · ${item.time}`:''}</small></div><span>›</span></div>`;
  function taskRow(item,key){const info=kind(item),priority=item.priority||'normal',mark=priority==='high'?'!':priority==='low'?'↓':'•';return `<div class="today-record-v30" data-task-id="${item.id}">${check(item,key)}<div class="today-record-main-v30" onclick="openTaskViewV31('${item.id}','${key}')"><div class="today-record-title-v30"><span class="record-kind-v30">${info.icon}</span><strong>${esc(item.title)}</strong><span class="priority-v40 ${priority}">${mark}</span></div><div class="today-record-period-v30"><span>▦</span><span>${esc(period(item))}</span></div></div><button type="button" class="menu-btn" onclick="event.stopPropagation();taskMenu('${item.id}','${key}')">⋮</button></div>`}
  function importantRow(entry){if(entry.kind==='task'){const item=state.tasks.find(x=>x.id===entry.id);if(!item)return '';return `<div class="plan-entry-v30 important-row-v31 important-v40" data-important-task-id="${item.id}" onclick="openTaskViewV31('${item.id}','${TODAY}')">${check(item,TODAY,'important-check-v40')}<div class="plan-entry-main-v30"><strong>${esc(item.title)}</strong><small>${esc(period(item))}</small></div><span class="important-mark-v40">!</span></div>`}return `<div class="plan-entry-v30 important-row-v31" onclick="${entry.action}"><span class="plan-entry-icon-v30">${entry.urgent?'!':'⚑'}</span><div class="plan-entry-main-v30"><strong>${esc(entry.title)}</strong><small>${esc(entry.meta||'')}</small></div><span>›</span></div>`}

  function cleanFinance(){document.querySelector('[data-page="finance"]')?.remove();document.querySelector('#finance')?.setAttribute('hidden','');document.querySelectorAll('#quick-dialog [data-create="expense"],#quick-dialog [data-create="income"]').forEach(x=>x.remove());const search=document.querySelector('#global-search');if(search)search.placeholder='Задачи, проекты, события, заметки…';document.querySelectorAll('#assistant .chip').forEach(x=>{if(/баланс|доход|расход|бюджет|финанс|платеж/i.test(x.textContent))x.remove()});document.querySelectorAll('#more .directory-row-v30').forEach(x=>{if(x.querySelector('.directory-name-v30')?.textContent.trim()==='Финансы')x.remove()})}
  const oldSetPage=setPage;setPage=function(target){oldSetPage(target==='finance'?'analytics':target)};window.setPage=setPage;
  const oldUpcoming=upcomingItems;upcomingItems=function(){return oldUpcoming().filter(x=>x.kind!=='finance')};window.upcomingItems=upcomingItems;
  const oldSearch=globalSearch;globalSearch=function(query){return oldSearch(query).filter(item=>{const action=String(item.action||'');return !['income','expense','budget','account','category'].some(type=>action.includes(`openEditor('${type}'`))&&!action.includes("setPage('finance')")})};window.globalSearch=globalSearch;
  const oldQuick=quickCommand;quickCommand=function(text){if(/баланс|доход|расход|бюджет|финанс|платеж/i.test(String(text||''))){toast('Финансовый модуль убран из приложения');return}return oldQuick(text)};window.quickCommand=quickCommand;
  const oldAreaOptions=areaOptions;areaOptions=function(selected=''){return oldAreaOptions(selected).replace(/<option value="finance"[^>]*>[^<]*<\/option>/,'')};window.areaOptions=areaOptions;

  function renderTodayV40(){const all=tasksForDate(TODAY),important=window.importantItemsV26?window.importantItemsV26():[],importantIds=new Set(important.filter(x=>x.kind==='task').map(x=>x.id)),tasks=all.filter(x=>kind(x).group==='tasks'),trips=all.filter(x=>kind(x).group==='trips'),events=all.filter(x=>kind(x).group==='events'),regular=tasks.concat(events).filter(x=>!importantIds.has(x.id)),regularTrips=trips.filter(x=>!importantIds.has(x.id)),done=tasks.filter(x=>taskDone(x,TODAY)).length,habits=state.habits.filter(x=>habitDue(x,TODAY)),habitDoneCount=habits.filter(x=>habitDone(x,TODAY)).length,workouts=workoutsForDate(TODAY),notes=state.notes.slice().sort((a,b)=>(Number(b.pinned)-Number(a.pinned))||((b.updatedAt||0)-(a.updatedAt||0))).slice(0,3);document.querySelector('#today').innerHTML=`<div class="today-icon-grid v40"><button type="button" class="today-icon-stat" onclick="setAnalyticsFocusV40('tasks')"><i>✓</i><b>${done}/${tasks.length}</b><small>задачи</small></button><button type="button" class="today-icon-stat" onclick="setAnalyticsFocusV40('trips')"><i>✈</i><b>${trips.length}</b><small>командировки</small></button><button type="button" class="today-icon-stat" onclick="setAnalyticsFocusV40('events')"><i>◉</i><b>${events.length}</b><small>события</small></button><button type="button" class="today-icon-stat" onclick="openMore('sport')"><i>↻</i><b>${habitDoneCount}/${habits.length}</b><small>привычки</small></button><button type="button" class="today-icon-stat" onclick="openMore('sport');setMoreTab('workouts')"><i>⚡</i><b>${workouts.length}</b><small>спорт</small></button></div>${important.length?section('Важное',important.length,important.map(importantRow).join('')):''}${section('Заметки',notes.length,notes.map(noteRow).join(''),'<button type="button" class="tiny-icon-btn" onclick="openEditor(\'note\')">＋</button>')}${section('Задачи, поручения и события',regular.length,regular.map(x=>taskRow(x,TODAY)).join(''),'<button type="button" class="tiny-icon-btn" onclick="openEditor(\'task\',null,{date:\''+TODAY+'\'})">＋</button>')}${regularTrips.length?section('Командировки',regularTrips.length,regularTrips.map(x=>taskRow(x,TODAY)).join('')):''}${workouts.length?section('Тренировки',workouts.length,workouts.map(workoutRow).join('')):''}`}

  const selectedDate=()=>state.selectedDate||TODAY;
  const monthStart=()=>{const d=new Date(`${selectedDate()}T12:00:00`);return new Date(d.getFullYear(),d.getMonth(),1,12)};
  function calendar(){const month=monthStart(),start=addDays(month,-((month.getDay()+6)%7)),cells=Array.from({length:42},(_,i)=>addDays(start,i));return `<div class="month-grid compact-month-v31">${['Пн','Вт','Ср','Чт','Пт','Сб','Вс'].map(x=>`<div class="dow">${x}</div>`).join('')}${cells.map(d=>{const key=dayKey(d),count=tasksForDate(key).length+workoutsForDate(key).length;return `<button type="button" class="month-day ${d.getMonth()!==month.getMonth()?'other':''} ${key===selectedDate()?'selected':''}" onclick="selectPlanDateV40('${key}')"><span class="num">${d.getDate()}</span><div class="dots">${Array.from({length:Math.min(count,3)},()=>'<i></i>').join('')}</div></button>`}).join('')}</div>`}
  function planTask(item,key){const info=kind(item);return `<div class="plan-entry-v30 plan-task-check-v32" data-plan-task-id="${item.id}" onclick="openTaskViewV31('${item.id}','${key}')">${check(item,key,'plan-check-v40')}<span class="plan-entry-icon-v30">${info.icon}</span><div class="plan-entry-main-v30"><strong>${esc(item.title)}</strong><small>${esc(period(item))} · ${esc(timeLabel(item))}</small></div><span>›</span></div>`}
  function renderPlanV40(){const month=monthStart(),key=selectedDate(),d=new Date(`${key}T12:00:00`),rows=[];tasksForDate(key).forEach(x=>rows.push({sort:x.startTime||x.time||'99:99',html:planTask(x,key)}));workoutsForDate(key).forEach(x=>rows.push({sort:x.time||'99:99',html:workoutRow(x)}));const entries=rows.sort((a,b)=>a.sort.localeCompare(b.sort)).map(x=>x.html).join('');document.querySelector('#plan').innerHTML=`<div class="plan-nav"><button type="button" onclick="movePlanMonthV40(-1)">‹</button><strong>${dateFmt(month,{month:'long',year:'numeric'})}</strong><button type="button" onclick="movePlanMonthV40(1)">›</button></div>${calendar()}<section class="simple-window-v30"><div class="simple-window-head-v30"><div><h3>Записи выбранного дня</h3><small class="selected-day-caption-v31">${dateFmt(d,{weekday:'long',day:'numeric',month:'long',year:'numeric'})}</small></div></div><div class="plan-month-list-v30">${entries||'<div class="simple-empty-v30">На выбранную дату записей нет.</div>'}</div></section>`}
  function selectPlanDateV40(key){state.selectedDate=key;save();render()}function movePlanMonthV40(step){const d=new Date(`${selectedDate()}T12:00:00`),target=new Date(d.getFullYear(),d.getMonth()+step,1,12),last=new Date(target.getFullYear(),target.getMonth()+1,0).getDate();state.selectedDate=dayKey(new Date(target.getFullYear(),target.getMonth(),Math.min(d.getDate(),last),12));save();render()}window.selectPlanDateV40=selectPlanDateV40;window.movePlanMonthV40=movePlanMonthV40;

  const analyticsStart=period=>period==='all'?(state.tasks.map(x=>x.date).filter(Boolean).sort()[0]||dayKey(addDays(today,-365))):dayKey(addDays(today,-Number(period||30)+1));
  const completed=(item,key)=>item.status==='completed'||(item.completedDates||[]).includes(key)||((item.recurrence?.type||'none')==='none'&&(item.completedDates||[]).length>0);
  function occurrences(period){const start=analyticsStart(period),rows=[];state.tasks.filter(x=>x.status!=='archived').forEach(item=>{const group=kind(item).group;if(!group)return;const recurrence=item.recurrence?.type||'none';if(recurrence==='none'){const due=item.endDate||item.date||TODAY;if(due>=start&&due<=TODAY)rows.push({item,key:item.date||due,due,group,done:completed(item,item.date||due)});return}let d=new Date(`${start}T12:00:00`),end=new Date(`${TODAY}T12:00:00`),guard=0;while(d<=end&&guard<740){const key=dayKey(d);if(taskOccursOn(item,key))rows.push({item,key,due:key,group,done:completed(item,key)});d=addDays(d,1);guard++}});return rows}
  function metric(rows,group){const list=rows.filter(x=>x.group===group),done=list.filter(x=>x.done).length,late=list.filter(x=>!x.done&&x.due<TODAY).length;return {group,total:list.length,done,late,pct:list.length?Math.round(done/list.length*100):0}}
  function categoryCard(m){const icon={tasks:'✓',trips:'✈',events:'◉'}[m.group],active=(state.analyticsFocusV40||'all')===m.group;return `<article class="category-v40 ${active?'active':''}" onclick="setAnalyticsFocusV40('${m.group}')"><div class="category-head-v40"><span class="category-icon-v40">${icon}</span><div><strong>${GROUP_NAME[m.group]}</strong><small>${m.done} ${GROUP_DONE[m.group]} из ${m.total}</small></div><span class="category-pct-v40">${m.pct}%</span></div><div class="progress-v40"><span style="width:${m.pct}%"></span></div><div class="category-foot-v40"><span>Осталось: ${Math.max(0,m.total-m.done)}</span><span class="${m.late?'late':''}">Просрочено: ${m.late}</span></div></article>`}
  const completionDate=row=>(row.item.completedDates||[]).slice().sort().at(-1)||row.due;
  function completedRow(row){const info=kind(row.item);return `<div class="completed-row-v40" data-analytics-task-id="${row.item.id}" onclick="openTaskViewV31('${row.item.id}','${row.key}')">${check(row.item,row.key,'analytics-check-v40')}<i>${info.icon}</i><div><strong>${esc(row.item.title)}</strong><small>${esc(info.name)} · ${dateLabel(completionDate(row))}</small></div><b>Готово</b></div>`}
  function trend(rows){const days=Array.from({length:7},(_,i)=>addDays(today,i-6)),values=days.map(d=>rows.filter(x=>x.done&&completionDate(x)===dayKey(d)).length),max=Math.max(1,...values);return `<section class="trend-v40"><h3>Выполнено за последние 7 дней</h3><div class="bars-v40">${days.map((d,i)=>`<div class="bar-wrap-v40"><b>${values[i]}</b><span class="bar-v40" style="height:${Math.max(4,Math.round(values[i]/max*64))}px"></span><small>${dateFmt(d,{weekday:'short'}).replace('.','')}</small></div>`).join('')}</div></section>`}
  function renderAnalyticsV40(){const periodValue=state.analyticsPeriodV40||'30',focus=state.analyticsFocusV40||'all',rows=occurrences(periodValue),metrics=['tasks','trips','events'].map(g=>metric(rows,g)),total=metrics.reduce((s,x)=>s+x.total,0),done=metrics.reduce((s,x)=>s+x.done,0),late=metrics.reduce((s,x)=>s+x.late,0),pct=total?Math.round(done/total*100):0,recent=rows.filter(x=>x.done&&(focus==='all'||x.group===focus)).sort((a,b)=>completionDate(b).localeCompare(completionDate(a))).slice(0,12);document.querySelector('#analytics').innerHTML=`<div class="analytics-tools-v40">${[['7','7 дней'],['30','30 дней'],['90','90 дней'],['all','Всё время']].map(([v,l])=>`<button type="button" class="${periodValue===v?'active':''}" onclick="setAnalyticsPeriodV40('${v}')">${l}</button>`).join('')}</div><div class="analytics-summary-v40"><article class="summary-v40"><small>Выполнено за период</small><strong class="good">${done}</strong></article><article class="summary-v40"><small>Общий процент выполнения</small><strong>${pct}%</strong></article><article class="summary-v40"><small>Осталось просроченных</small><strong class="${late?'bad':'good'}">${late}</strong></article></div><div class="analytics-tools-v40">${[['all','Все'],['tasks','Задачи'],['trips','Командировки'],['events','События']].map(([v,l])=>`<button type="button" class="${focus===v?'active':''}" onclick="setAnalyticsFocusV40('${v}')">${l}</button>`).join('')}</div><div class="category-grid-v40">${metrics.map(categoryCard).join('')}</div>${trend(rows)}<div class="section-head"><h3>Последние выполненные</h3><span class="counter">${recent.length}</span></div><div class="completed-v40">${recent.length?recent.map(completedRow).join(''):'<div class="simple-empty-v30">За выбранный период выполненных записей нет.</div>'}</div>`}
  function setAnalyticsPeriodV40(value){state.analyticsPeriodV40=value;save();render()}function setAnalyticsFocusV40(value){state.analyticsFocusV40=value;save();if(page!=='analytics')setPage('analytics');else render()}window.setAnalyticsPeriodV40=setAnalyticsPeriodV40;window.setAnalyticsFocusV40=setAnalyticsFocusV40;

  function widgetPayload(){const open=tasksForDate(TODAY).filter(x=>!taskDone(x,TODAY)),tasks=open.filter(x=>kind(x).group==='tasks'),trips=open.filter(x=>kind(x).group==='trips'),events=open.filter(x=>kind(x).group==='events'),ids=new Set((window.importantItemsV26?window.importantItemsV26():[]).filter(x=>x.kind==='task').map(x=>x.id));open.filter(x=>x.priority==='high').forEach(x=>ids.add(x.id));return {tasks:tasks.length,trips:trips.length,events:events.length,important:ids.size,taskMeta:`${tasks.filter(x=>x.priority==='high').length} важные`,tripMeta:trips.length?'сегодня':'нет',eventMeta:events.length?'сегодня':'нет',importantMeta:ids.size?'под контролем':'спокойно',updated:Date.now()}}
  function syncWidget(){try{if(typeof Android!=='undefined'&&typeof Android.updateWidget==='function')Android.updateWidget(JSON.stringify(widgetPayload()))}catch(error){console.error(error)}}window.syncShtabWidgetV40=syncWidget;window.openFromNativeV40=function(target='today',focus=''){setPage(target);if(target==='analytics'&&focus)setAnalyticsFocusV40(focus)};
  const oldSave=save;save=function(){oldSave();syncWidget()};window.save=save;
  headings.today=['Сегодня','Задачи, события и планы дня'];headings.plan=['План','Календарь и записи выбранного дня'];headings.analytics=['Аналитика','Выполнение задач, событий и командировок'];
  const oldRender=render;render=function(){oldRender();cleanFinance();if(page==='finance')page='analytics';renderTodayV40();renderPlanV40();renderAnalyticsV40();document.querySelectorAll('.page').forEach(x=>x.classList.toggle('active',x.id===page));document.querySelectorAll('.nav-item').forEach(x=>x.classList.toggle('active',x.dataset.page===page));const h=headings[page]||headings.today;document.querySelector('#page-title').textContent=h[0];document.querySelector('#page-subtitle').textContent=h[1];if(page==='assistant')cleanFinance();if(page==='more'&&state.moreView==='projects')cleanFinance();const footer=[...document.querySelectorAll('#more p')].find(x=>x.textContent.includes('Штаб AI'));if(footer)footer.textContent='Штаб AI · 4.0.0';document.querySelectorAll('button:not([type])').forEach(x=>x.type='button');syncWidget()};window.render=render;
  window.__shtabDiagnostics={...(window.__shtabDiagnostics||{}),uiVersion:()=> '4.0.0',financeVisible:()=>Boolean(document.querySelector('[data-page="finance"]')||!document.querySelector('#finance')?.hidden),analyticsCategoryCount:()=>document.querySelectorAll('#analytics .category-v40').length,analyticsSummaryCount:()=>document.querySelectorAll('#analytics .summary-v40').length,widgetPayload:()=>widgetPayload(),quickFinanceButtons:()=>document.querySelectorAll('#quick-dialog [data-create="expense"],#quick-dialog [data-create="income"]').length};
  render();
})();
//# sourceURL=chunk30.js
